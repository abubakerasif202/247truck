import { notFound } from 'next/navigation';
import { PageHeader } from '@/components/ui/page-header';
import { getCurrentAccess } from '@/lib/auth/access';
import { hasPermission } from '@/lib/auth/permissions';
import { createServerSupabaseClient } from '@/lib/supabase/server';
import { cancelJobAction, completeJobAction, transitionJobAction } from '../actions';
export default async function JobPage({ params }: { params: Promise<{ id: string }> }) { const access = await getCurrentAccess(); if (!hasPermission(access, 'jobs.view')) return <PageHeader title="Job" subtitle="Permission denied" />; const { id } = await params; const { data, error } = await (await createServerSupabaseClient()).rpc('job_detail', { p_job_id: id }); if (error || !data) notFound(); const actions = data.status === 'completed' || data.status === 'cancelled' ? null : <div className="flex flex-wrap gap-2"><a href={`/jobs/${id}/edit`} className="flex h-10 items-center rounded-md border px-4 text-sm">Edit</a>{data.status === 'new' ? <form action={transitionJobAction.bind(null, id, data.version, 'in_progress')}><button className="h-10 rounded-md bg-primary px-4 text-sm text-primary-foreground">Start job</button></form> : null}<form action={cancelJobAction.bind(null, id, data.version)}><button className="h-10 rounded-md border px-4 text-sm">Cancel</button></form>{hasPermission(access, 'jobs.complete') ? <form action={completeJobAction.bind(null, id, data.version)}><button className="h-10 rounded-md bg-primary px-4 text-sm text-primary-foreground">Complete job</button></form> : null}</div>; return <div className="operations-page max-w-5xl"><PageHeader title={data.job_number} subtitle={`${data.customer_snapshot?.display_name ?? 'Walk-in'} · ${String(data.status).replaceAll('_', ' ')}`} actions={actions} /><div className="grid gap-3 rounded-xl border bg-card p-5">{(data.lines ?? []).map((line: Record<string, unknown>) => <div key={String(line.id)} className="flex justify-between border-b py-2 text-sm last:border-0"><span>{String(line.description)} · {String(line.quantity)}</span><span>{line.unit_price_incl_gst == null ? 'PRICE PENDING' : `$${Number(line.line_total_incl_gst).toFixed(2)}`}</span></div>)}<div className="pt-3 text-right font-semibold">{data.total_incl_gst == null ? 'PRICE PENDING' : `$${Number(data.total_incl_gst).toFixed(2)} incl GST`}</div></div></div>; }
