/**
 * Name of the HTTP-only cookie that stores an Admin's chosen location scope.
 * Never used for Manager authorisation — Managers derive scope from their
 * profile.
 */
export const LOCATION_SCOPE_COOKIE = 'inventory_location_scope';
